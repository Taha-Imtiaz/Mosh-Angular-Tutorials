// Video # 20 and Video # 21
export class Point {
    // fields
//     private x: number;
//    private y: number;

    // constructor(x: number, y: number) {
    //     this.x = x;
    //     this.y = y
    // }

    // constructor with optional parameters
    // constructor(x?: number, y?: number) {
    //     this.x = x;
    //     this.y = y
    // }

    // constructor creates fields and initialize them if we prefix constructor with an access modifier
    //  constructor(private _x?: number, private _y?: number) {
      
    // }

    // methods (because thee functions are a part of a class)
    // drawPoint() {
    //     // ...add logic for drawing a point
    //     console.log(`X is : ${this._x} Y is : ${this._y}`);

    // }

    // deine getters and setters
    // get x() {
    //     return this._x
    // }

    // set x(value) {
    //     if(value < 0) {
    //         throw new Error('value cannot be less than zero!')
    //     }
    //     this._x = value
    // }

    // getDistance(another: Point) {
    //     // get distance between this point and a point coming from a parameter
    // }
}